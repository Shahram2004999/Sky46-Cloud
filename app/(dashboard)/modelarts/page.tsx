'use client'\n\nimport { useState } from 'react'\n\nexport default function ModelArtsPage() {\n  const [activeTab, setActiveTab] = useState('datasets')\n\n  return (\n    <div className=\"space-y-6 p-6\">\n      <div>\n        <h1 className=\"text-3xl font-bold\">ModelArts</h1>\n        <p className=\"text-slate-400 mt-2\">End-to-end machine learning platform</p>\n      </div>\n\n      <div className=\"glass-effect rounded-lg p-6\">\n        <div className=\"flex gap-4 border-b border-slate-700 mb-6\">\n          {['datasets', 'jobs', 'models', 'deployments', 'endpoints'].map((tab) => (\n            <button\n              key={tab}\n              onClick={() => setActiveTab(tab)}\n              className={`px-4 py-2 text-sm font-medium transition-colors ${\n                activeTab === tab\n                  ? 'border-b-2 border-red-500 text-white'\n                  : 'text-slate-400 hover:text-slate-200'\n              }`}\n            >\n              {tab.charAt(0).toUpperCase() + tab.slice(1)}\n            </button>\n          ))}\n        </div>\n\n        {activeTab === 'datasets' && (\n          <div className=\"text-center py-8 text-slate-400\">\n            <p>Datasets automatically imported from OBS buckets</p>\n            <p className=\"text-sm mt-2\">Create OBS buckets and upload files to get started</p>\n          </div>\n        )}\n        {activeTab === 'jobs' && (\n          <div className=\"text-center py-8 text-slate-400\">\n            <p>No training jobs yet</p>\n            <p className=\"text-sm mt-2\">Create a new training job from the ModelArts dashboard</p>\n          </div>\n        )}\n        {activeTab === 'models' && (\n          <div className=\"text-center py-8 text-slate-400\">\n            <p>No registered models yet</p>\n            <p className=\"text-sm mt-2\">Models will appear here after training jobs complete</p>\n          </div>\n        )}\n        {activeTab === 'deployments' && (\n          <div className=\"text-center py-8 text-slate-400\">\n            <p>No active deployments</p>\n            <p className=\"text-sm mt-2\">Deploy your registered models to create endpoints</p>\n          </div>\n        )}\n        {activeTab === 'endpoints' && (\n          <div className=\"text-center py-8 text-slate-400\">\n            <p>No endpoints available</p>\n            <p className=\"text-sm mt-2\">Deploy models to create testable endpoints</p>\n          </div>\n        )}\n      </div>\n    </div>\n  )\n}\n